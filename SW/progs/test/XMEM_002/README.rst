tests EXTERNAL RAM as XMEM - automatic mapping is done to fill addresses 0x2200..0xFFFF from external chip - we will use upper half for this (XAA16 = high) so the rest is continous as 0..11FFF for using it manually
use speed 115200
